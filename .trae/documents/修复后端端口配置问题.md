1. 停止当前运行的后端服务器进程
2. 重新启动后端服务器（从 `x:\day1\backend` 目录运行 `npm run start:dev`）
3. 验证后端是否在 3001 端口正确启动
4. 测试前端 API 请求是否能够成功连接到后端

**原因**：

* 后端配置文件（`.env`）设置端口为 3001

* 前端配置文件（`.env`）也请求 3001 端口

* 但实际后端进程可能运行在 3000 端口（可能是之前启动时没读取环境变量）

* 需要重启后端以应用正确的端口配置

