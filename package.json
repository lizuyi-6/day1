{
    "name": "day1-aether-flow",
    "version": "1.0.0",
    "engines": {
        "node": "18.x"
    },
    "scripts": {
        "dev:backend": "cd backend && npm run start:dev",
        "dev:frontend": "cd frontend && npm run dev",
        "install:all": "cd backend && npm install && cd ../frontend && npm install"
    }
}